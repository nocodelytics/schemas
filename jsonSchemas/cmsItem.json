{
  "title": "cmsItem",
  "description": "A raw cms item from the database",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time"
    },
    "collection_id": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "slug": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  },
  "required": ["id", "created_at", "collection_id", "name", "slug"]
}
